import emailValidator from "./emailValidator";
import currencyMask from "./currency-mask";

const utils = { emailValidator, currencyMask };

export { emailValidator, currencyMask };

export default utils;
